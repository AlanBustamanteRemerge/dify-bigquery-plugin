version: 1.0.56
type: plugin
author: testuser
name: testplugin
label:
  en_US: BigQuery Minimal Test
# icon: _assets/icon.svg
provider: minimal_provider.yaml # Path relative to package root (now at root)
meta:
  version: 0.0.1 # This is the API version, not the plugin package version
  runner:
    language: python
    version: '3.12'
    entrypoint: main
# created_at: # Removed
# privacy: PRIVACY.md # Optional for local testing 